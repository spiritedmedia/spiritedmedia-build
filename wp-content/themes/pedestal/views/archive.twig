{% extends 'layouts/layout--content-sidebar.twig' %}

{% block content %}

    {% if archive_title %}
    <header class="c-main__header js-main-header">
        <h1 class="c-main__title">{{ archive_title }}</h1>
    </header>
    {% endif %}

    {% if stream %}
        <section class="stream {{ extra_stream_container_classes }}">
            <div class="stream__items">
                {{ stream }}
            </div>
        </section>

        {{ pagination }}

    {% endif %}

{% endblock %}
