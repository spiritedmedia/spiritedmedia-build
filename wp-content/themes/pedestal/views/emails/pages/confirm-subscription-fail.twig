{% extends 'layouts/layout--content.twig' %}

{% block content %}
<div id="subscribe-to-newsletter-page" class="subscription-confirmation-page">

    <h3 class="js-error-message-text">The confirmation link appears to be bad. Please sign up again.</h3>

    {% import "macros/macros.twig" as macros %}
    {% if provided_list_ids %}
    <form action="{{ site.url }}/subscribe-to-email-list/" method="POST" data-ga-category="Submitted Form" data-ga-label="Confirm Subscription Fail|List IDs:{{ list_id_str }}">

        <div class="js-form-fields">
            <fieldset>
                {% if is_newsletter %}
                    {% include 'partials/email-forms/form-daily-legend.twig' %}
                    {% include 'partials/email-forms/form-daily-input-address.twig' %}
                {% endif %}

                {% if is_cluster %}
                    <legend>Follow {{ subscription_title }}</legend>
                    {% include 'partials/email-forms/form-follow-cluster-input-address.twig' %}
                    {% for list_id in list_ids %}
                        <input type="hidden" name="list-ids[]" value="{{ list_id|e('html_attr') }}">
                    {% endfor %}

                {% endif %}

                {% include 'partials/email-forms/form-honeypot.twig' %}
            </fieldset>
            <button class="btn btn--large form-submit expand js-form-submit" type="submit">
                {% include 'partials/email-forms/form-submit-signup-inner.twig' %}
            </button>
            <p><a href="mailto:{{ site.emails.contact }}?subject={{ help_text|url_encode }}">Having trouble? Email {{ site.emails.contact }}</a></p>
        </div>

        <div class="success-message js-success-message">
            {% include 'partials/email-forms/form-daily-subscribed.twig' %}
        </div>

    </form>
    {% endif %}
</div>
{% endblock %}
