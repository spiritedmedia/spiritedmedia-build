{% extends 'layouts/layout--content.twig' %}

{% block content %}
<article class="c-profile  c-main__content">
    {{ macros.user_card( _context, user, {
        'format': 'extended',
        'img_size': 'medium-square',
        'posts': false,
        'stream': stream,
        'pagination': pagination,
        'upper_pagination': upper_pagination,
        'show_closure_rule': show_closure_rule
    } ) }}
</article>

{% endblock %}
